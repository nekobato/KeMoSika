import type { LayoutData } from "@shared/types";
import layout from "./json/ansi75.json";

export const displayOrder = 6;

export const ansi75Layout: LayoutData = layout as LayoutData;

export default ansi75Layout;
