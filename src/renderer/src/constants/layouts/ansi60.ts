import type { LayoutData } from "@shared/types";
import layout from "./json/ansi60.json";

export const displayOrder = 5;

export const ansi60Layout: LayoutData = layout as LayoutData;

export default ansi60Layout;
