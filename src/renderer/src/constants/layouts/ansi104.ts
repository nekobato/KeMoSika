import type { LayoutData } from "@shared/types";
import layout from "./json/ansi104.json";

export const displayOrder = 4;

export const ansi104Layout: LayoutData = layout as LayoutData;

export default ansi104Layout;
